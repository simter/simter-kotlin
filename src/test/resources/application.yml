spring.main.banner-mode: 'off'
logging:
  file.name: target/test.log
  level: # TRACE < DEBUG < INFO < WARN < ERROR < FATAL < OFF
    root: ERROR
    tech.simter.kotlin: WARN
simter:
  kotlinx-serialization:
    class-discriminator: "#class" # kotlinx-serialization default 'type'
